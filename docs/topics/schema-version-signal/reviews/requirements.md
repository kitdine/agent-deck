---
status: active
topic: schema-version-signal
subject: requirements.md
---

# Review log — schema-version-signal / requirements.md

## Round 1 — 2026-09-05
- Reviewed state: HEAD `8d283cd960af71ce9f99e63247f96810c1dd5a98`; `requirements.md` blob `fe725f271ec26f337401aa76a177681842482284` (the topic directory is still untracked, so the blob hash is the only content identity available)
- Reviewer: Claude Code
- Method: Formal design/contract review using the `development-workflow` Review dimensions for a design target. Every claim the document makes about existing code, contracts, or released binaries was checked against this repository at the reviewed HEAD; the six-row measurement table was re-executed where it could be reproduced locally. `scripts/check-topic-docs.sh` is the project's document-set checker; it audits the declared set rather than one document's boundary, so it belongs to the `tasks.md` review and is not this target's checker — it was run anyway and reported only the two deliberately unwritten rows (`ux/menubar-schema-signal.md`, `architecture.md`), which is the state `tasks.md` declares. The project ships no requirements-boundary checker.
- Scope: `docs/topics/schema-version-signal/requirements.md` only. Premise checks against `internal/store/store.go`, `internal/store/migrations.go`, `internal/doctor/doctor.go`, `internal/desktop/desktop.go`, `cmd/agentdeck/main.go`, `apps/macos/AgentDeckShared/DesktopWire.swift`, `packaging/homebrew/agentdeck-app.rb.tmpl`, `docs/specs/cli-design.md` (version 28), and the existing regression tests for the `future` schema case.
- Findings:
  - [P1] R1-F1 — The topic contradicts a rule `docs/specs/cli-design.md` states today, and never says so. Its Goals ("The report carries a recovery the user can act on") and Acceptance item 3 ("The recovery text names upgrading AgentDeck") require a recovery for this condition, while `cli-design.md:2282` states "A schema newer than the binary remains an `unknown_schema` error" and `cli-design.md:2289-2290` fixes the shared quick/full schema-state matrix as "a future schema reports `unknown_schema` **without a recovery command**". Acceptance item 7 describes the whole contract change as documenting "the code alongside the existing error table", which is a different edit in a different section and leaves the contradicting sentence standing. Two existing regression cases pin the current rule with an empty recovery expectation — `internal/doctor/doctor_test.go:349` and `cmd/agentdeck/main_test.go:1461`, both `{"future", "database", "error", "unknown_schema", "", 99, 0, false}` — and Acceptance item 9 only adds new tests, so nothing in the boundary accounts for revising them. An implementer reading this boundary can satisfy item 7 exactly and still ship a specification that forbids the recovery item 3 requires -> open; state in the boundary that this topic revises the `cli-design.md` doctor section's no-recovery rule and its schema-state matrix, and that the existing `future` regression expectations change with it.
  - [P1] R1-F2 — "What is missing is the value, not the mechanism" is true for the recovery string and false for the two version numbers, which is the harder half of the requirement. `doctor.Check` carries `name`, `status`, `code`, `count`, `recovery_command` (`internal/doctor/doctor.go:23-29`) — one integer, not two — and the `schema_outdated` path the document cites as proof reports only the on-disk version (`internal/doctor/doctor.go:82`, `Count: version`), never the supported one. So the asymmetry section demonstrates a mechanism for one number and a recovery, while Goals and Acceptance item 2 require both the on-disk and the supported version to be readable "from the documented JSON contract without parsing a message string". Satisfying item 2 needs a new field, and that field is not confined to the CLI: `desktop.HealthCheck` (`internal/desktop/desktop.go:214-220`) mirrors the same five keys into the desktop snapshot, and `DesktopHealthCheckV1` (`apps/macos/AgentDeckShared/DesktopWire.swift:941-953`) decodes them key by key. The boundary states no position on adding a contract field, and its "mechanism already exists" claim actively suggests none is needed -> open; either declare the new contract field in scope (naming the CLI JSON and desktop wire surfaces it touches) or narrow item 2 to what the existing keys can carry.
  - [P2] R1-F3 — Two of the three source locations in "Four distinct defects" no longer resolve. `store.go:199` is `internal/store/store.go:202` and `migrations.go:295` is `internal/store/migrations.go:317` at the reviewed HEAD; both were correct at the drafting baseline `276986e` (verified with `git show`). `doctor.go:66-70` is still exact. The behavior described at each location is correct — only the coordinates are stale, and `architecture.md` is the document that will copy them -> open; cite the symbols (`OpenReadOnly`'s version check, `migrate`'s version check) or refresh the line numbers and name the baseline SHA they belong to.
  - [P2] R1-F4 — The provenance table labels a development build as a release. Its first row reads "v0.5.0 / `735d010`, built 2026-08-25", but `735d010` is an ordinary `main` commit from 2026-08-24 (`feat(macos): count release candidates`) whose `CurrentSchemaVersion` is 18, while the released `v0.5.0` is `acb8384` with `CurrentSchemaVersion` 23. Read today, the table asserts that released `v0.5.0` supports schema 18, which is exactly the inference the surrounding argument invites; the third row's "current main / 21" has drifted the same way (`main` is now `8d283cd` at 23). The `v0.4.1` row is accurate (`3b709a8`, 18). Neither error weakens the topic — the paragraph that follows already argues the condition is reachable without any development build — but the table is the measurement the boundary rests on -> open; label the first row as a pre-release build carrying the `v0.5.0` string and state the measurement baseline for the third.
- Evidence: Built the reviewed HEAD (`go build ./cmd/agentdeck`) and reproduced three of the six measured rows against a throwaway state directory whose `schema_metadata.version` was set to 999. `doctor --format json` returned exactly three checks (`state_permissions`, `state_lock`, `database`/`unknown_schema` with no `count` and no `recovery_command`) with envelope `partial:false`, against twelve checks and `partial:false` on the healthy real store — confirming defect 3 in both halves, including that `partial` is an unconditional envelope field (`internal/output/output.go:14`) that `writeResult` never sets (`cmd/agentdeck/main.go:3654-3657`). `provider list --format json` returned `{"code":"runtime_error","message":"unknown_schema: database version 999 exceeds supported version 23"}`, confirming defect 1: `errorCode` (`cmd/agentdeck/main.go:336-375`) has no `ErrUnknownSchema` branch and falls through to the residual. `desktop snapshot --format json` returned `partial:true` with `provider_unavailable` and `usage_unavailable` among its warnings and the cause only inside `health.checks`, as measured. The `state_busy` precedence claim was verified by inspection instead: `open` acquires the state lock before `migrate` reaches the version comparison (`internal/store/store.go:207-215`), while `OpenReadOnly` needs no lock. The Hook row was verified by inspection: `runUsageHookEvent` returns `nil` on a failed `openStore` (`cmd/agentdeck/main.go:2951-2954`), and `openStore` goes through `store.Open`, which migrates. `session list` was confirmed to use `store.OpenSessions` on `sessions.sqlite3` and never to touch the core store (`cmd/agentdeck/main.go:2131`, `internal/store/store.go:36`). Cross-references check out: `docs/fixes/claude-no-route-quality.md` and `docs/archive/topics/cli-error-classification/requirements.md` exist, the Cask claims match `packaging/homebrew/agentdeck-app.rb.tmpl:23-27`, the desktop-update withdrawal is in `docs/roadmap.md`, and Beads `ad-etp` is `closed`. The Surfaces section lists `ux/menubar-schema-signal.md` by path and matches the Documents matrix in `tasks.md`. `scripts/check-topic-docs.sh` reported only the two unwritten declared documents.
- Completion gate: NOT_VERIFIED — a document boundary is crossed only at `Verdict: PASS` (`.agent-instructions/evidence.md`), so this round records no CEv1 upsert for `document` / `schema-version-signal:requirements.md`.
- Verdict: REOPEN

Matrix note: `tasks.md`'s Documents matrix keeps `requirements.md` as `Draft [x]`, `Review [ ]`. In that file an unticked `Draft` is explicitly defined as "the document is required and not yet written", so unticking it to express this REOPEN would assert something false about a document that exists. The REOPEN is carried by this record and by the Beads task returning to `in_progress`.

## Round 2 — 2026-09-06
- Reviewed state: HEAD `8d283cd960af71ce9f99e63247f96810c1dd5a98` (unchanged); `requirements.md` blob `b0854d0057df1dfd5bd07f6cec57e5c523f7333f`, repaired from Round 1's `fe725f271ec26f337401aa76a177681842482284`. The topic directory is still untracked.
- Reviewer: Claude Code
- Method: Re-review of the four Round 1 findings against the changed document, per `development-workflow`'s Re-review contract. Every source coordinate, contract line reference, and version claim the repair *introduced* was verified against the repository at the same HEAD — a repair that fixes a stale citation by adding a wrong one closes nothing. Round 1's reproduction evidence was reused unchanged: neither the binary nor the store behavior it measured was touched, and the document's measured-behavior tables are byte-identical apart from the provenance wording R1-F4 required.
- Scope: `docs/topics/schema-version-signal/requirements.md`, all four prior findings, plus the new `Contract changes` section and the rewritten `The asymmetry that shows the intent` section that the repair introduced.
- Findings:
  - R1-F1 closed: the repair adds a `Contract changes` section that names both contradicting sentences by line — `cli-design.md:2282` and the quick/full matrix at `cli-design.md:2289-2290` — states that they are rewritten in the same pass, and records that the stable `error.code` table at `cli-design.md:2162-2184` carries no row for this condition, so adding one there is a separate edit that does not touch either sentence. Acceptance item 7 now requires both halves explicitly and says that documenting the code beside the table does not satisfy it alone. Goals item 3 carries the same statement. All four line ranges verified: the table header is `cli-design.md:2162` and its `runtime_error` row is `:2184`; `### Error-Code Compatibility` opens at `:2200` and the compatibility break paragraph runs to `:2220`.
  - R1-F2 closed: `The asymmetry that shows the intent` is split into the two halves it was conflating — the recovery string is a missing value (`Check` already carries `Recovery`), and the two version numbers are a missing mechanism, stating that `Check` carries one `Count`, that `schema_outdated` fills it with the stored version alone, and that no check emits the supported version at all. `Contract changes` puts the addition in scope across `internal/doctor/doctor.go:23-29`, `internal/desktop/desktop.go:214-220`, `apps/macos/AgentDeckShared/DesktopWire.swift:941-953`, and the `cli-design.md` JSON contract, leaving the field's name, count, and wire-version additivity to `architecture.md`. Goals items 2 and 6 and Acceptance item 2 were reworded to match. It also closes the gap Round 1 did not raise: Acceptance item 9 now requires the two existing `future` cases to assert the new expectations.
  - R1-F3 closed: the section opens with a baseline statement — coordinates are line numbers at `8d283cd` — and every citation now names its owning symbol. Verified at that HEAD: `OpenReadOnly`'s version check is `internal/store/store.go:200-202`, `migrate`'s is `internal/store/migrations.go:316-317`, and `Service.Check` (defined at `internal/doctor/doctor.go:49`) short-circuits at `:66-70`.
  - R1-F4 closed: the provenance table's first row now reads as a pre-release build of `main` at `735d010` stamped with the then-unreleased `v0.5.0` string, the second names released `v0.4.1`, and the third is labelled a measurement baseline. A following paragraph states the distinction outright. Every new claim verified: `v0.5.0-rc.1` is `40d52ab` (2026-09-01), so `735d010` (2026-08-24) does predate every release candidate; released `v0.5.0` is `acb8384` (2026-09-04) at `CurrentSchemaVersion` 23; `main` is `8d283cd` at 23; `internal/buildinfo/buildinfo.go` does hold a link-time `Version` stamp defaulting to `dev`, which is the mechanism the row now cites.
  - No new findings. The repair added one section and rewrote four passages; it introduced no claim about existing code or contracts that fails verification, and no statement that contradicts another section.
- Evidence: `git hash-object` for both blobs; `git show 735d010:internal/store/store.go` and `3b709a8:…` reconfirming `CurrentSchemaVersion = 18` for both installed binaries, `git log -1` on `v0.5.0-rc.1`, `v0.5.0`, and `735d010`; `sed -n` on each cited range in `internal/store/store.go`, `internal/store/migrations.go`, `internal/doctor/doctor.go`, `internal/desktop/desktop.go`, `apps/macos/AgentDeckShared/DesktopWire.swift`, `internal/doctor/doctor_test.go`, `cmd/agentdeck/main_test.go`, and `docs/specs/cli-design.md`. Round 1's fixture reproduction (`schema_metadata.version = 999` against a current build: `doctor` three checks with `partial:false`, `provider list` `runtime_error`, `desktop snapshot` `partial:true` with the cause only in `health.checks`) was not re-run — nothing it measured changed — and remains the evidence behind defects 1, 3, and the measured-behavior table.
- Completion gate: VERIFIED — `document` / `schema-version-signal:requirements.md`, `target_content_state` `head=8d283cd960af71ce9f99e63247f96810c1dd5a98;document=b0854d0057df1dfd5bd07f6cec57e5c523f7333f` (`subject_digest` `f28036a6a07568eb040873f1e3612e0074233e23741275bbe12bae2f9a88ff2d`). Recorded after this round's status synchronization as four lowercase CEv1 nodes plus `requires`/`satisfies`/`observed_at`; the re-run gate query returns the single required criterion satisfied by one `outcome: pass` evidence node bound to that exact state. No prior record for this WorkUnit existed, so nothing was superseded.
- Verdict: PASS
