---
status: historical
plan: shell-integration
task: cross-shell-acceptance
retired: 2026-07-31
---

# Review log — shell-integration / cross-shell-acceptance

## Round 1 — 2026-07-30

- Reviewed state: `a75a0c6` plus reviewed file-set SHA-256 `96760c0fa74c2187f5ae7a2fa31608014e5989aed1211b66be5aca2c8a1224c5`
- Reviewer: Claude Opus 5
- Scope: `cmd/agentdeck/main.go`, `cmd/agentdeck/shell_init_test.go`, `scripts/test-completion-install.sh`, `scripts/test-release-distribution.sh`, `scripts/manage-install.sh`, `packaging/homebrew/agentdeck.rb.tmpl`, `README.md`, `docs/specs/cli-manual.md`, `docs/plans/shell-integration.md`; Task 5's twelve acceptance bullets, the PATH-guard fix folded in on 2026-07-30, and the measurement plus its mandated backfill
- Findings:
  - [P1] The task's named deliverable — isolated-home cross-shell acceptance — is largely not delivered, yet `Dev` is ticked. The change set adds Claude fail-open coverage and the `unavailable`-mode stderr assertions, and nothing else. `scripts/test-completion-install.sh` still never starts a shell that loads the **managed block from a startup file**: all sixteen wrapper checks `source` the generated script directly, and every `shell setup`/`remove` case compares file bytes. The only interactive shell startups in the file (`:268`, `:271`) are the completion checks inherited from the installer tests. So none of these acceptance bullets has isolated-home integration coverage: a fresh shell loading `codex` and `claude` as functions from an installed block; a single no-argument `shell setup` configuring two shells in a home and leaving the third alone; the printed deactivation command succeeding when the functions are absent — which the plan singles out because `unfunction codex claude` fails in zsh for a non-function; a fresh shell after `remove` resolving the real commands; the task 6 advisories appearing in a real shell; the first interactive `--via` switch configuring the shell with no `shell setup` run at any point; the same switch under `--quiet` writing no startup file; and a fresh shell starting silently with the block installed and `agentdeck` off `PATH`. Several are covered by Go tests from tasks 2, 3, and 7, but this task exists precisely because unit coverage of the editor and the renderer is not evidence that a real shell startup works, and the plan routes the `v0.2.1-rc.2` gate through it. Note one genuine obstacle to record rather than skip silently: the interactive `--via` bullet needs a pty, because the terminal check is a Go variable that a plain script invocation always sees as non-interactive — drive it through `script -q` or an equivalent, or state in the plan why it is deferred and what covers it instead.
  - [P2] The measured figures were pasted verbatim into the Homebrew caveat, the installer narration, the README, and the manual, and they are sandbox-specific by an order of magnitude. `brew info` will now tell every user, on any hardware, that an eligible route costs "185.84 ms total (+152.02 ms)". Measured independently on this same host, a bare `fork`/`exec` of a no-op `/bin/sh` client is already ~10.6 ms per invocation against a normal host's ~1-2 ms, so the whole table is inflated 5-20x by the measuring environment and reads as a property of AgentDeck rather than of the sandbox. The caveat also now spends six of its twelve lines on a benchmark table. Related and independent of representativeness: the gated path measured *faster* than the direct baseline (31.62 ms versus 33.83 ms), so the noise floor exceeds the effect being reported, and "no measurable added cost" is being derived from a negative delta rather than from the structural fact that the gated path starts no process at all. The plan does instruct this backfill (`docs/plans/shell-integration.md:1068-1070`), so this is a plan-versus-outcome conflict to settle, not a deviation: keep the precise table in the plan, where the host and method are described, and in the four user-facing texts state the shape of the cost — no AgentDeck process while the marker is absent, one extra process plus one read-only database open while it is present — with at most an order-of-magnitude figure whose host is named. Amend the plan's follow-up wording to match whichever way this is decided.
- Test review:
  - The PATH-guard fix routed here on 2026-07-30 is properly closed. All four generated wrapper bodies now guard (`command -v agentdeck >/dev/null 2>&1` for bash and zsh, `type -q agentdeck` for fish), `TestShellInitEmitsDynamicClientWrappersForSupportedShells` asserts exactly two guards per script per shell, and `scripts/test-completion-install.sh:207-243` captures stderr for both clients across all three shells and requires it empty in `unavailable` mode. Leaving `nonzero` and `empty` unasserted is correct: a present-but-failing AgentDeck must not be silenced. Confirmed by running the suite — the `Unknown command` noise that previously appeared in `make check-install` output is gone.
  - `TestShellInitQuotesGatePathAndDefinesBothWrappers` was correctly generalized from prefix matching to substring search so it still finds the gate expression now that the guard precedes it.
  - The gap is the missing fresh-shell layer described above, not the quality of what was added.
- Evidence:
  - `GOCACHE=/private/tmp/agent-deck-go-build go test -count=1 -mod=vendor ./...` passed.
  - `GOCACHE=/private/tmp/agent-deck-go-build go vet -mod=vendor ./...` passed.
  - `GOCACHE=/private/tmp/agent-deck-go-build make check-install` passed, with no `Unknown command` line in its output.
  - `GOCACHE=/private/tmp/agent-deck-go-build make check-release-distribution` passed.
  - Independent baseline for the P2: 5 batches of 200 `fork`/`exec` calls to a no-op `/bin/sh` script on this host gave a median of 10.56 ms per invocation.
  - `grep` over `scripts/*.sh` confirms the only interactive shell startups are the two completion checks; all wrapper coverage sources the generated script directly.
- Verdict: REOPEN

## Round 2 — 2026-07-30

- Reviewed state: `a75a0c6` plus reviewed file-set SHA-256 `ae940bc7f93dd12bdf33587b6556fd50b3fc5b65c77b5b2a59c6f6ba50fc4859`
- Reviewer: Claude Opus 5
- Scope: Round 1's two findings; `scripts/test-shell-integration-acceptance.sh`, `Makefile`, `cmd/agentdeck/main.go`, `cmd/agentdeck/shell_init_test.go`, `docs/plans/shell-integration.md`, and the four disclosure texts with both assertion sets
- Finding resolution:
  - [CLOSED] The isolated-home acceptance layer now exists as `scripts/test-shell-integration-acceptance.sh`, run by `make check-install`. It drives real interactive shells through a pty for bash, zsh, and fish, loading the managed block from each shell's own startup file (`--rcfile` for bash, `ZDOTDIR` for zsh, `XDG_CONFIG_HOME` for fish), and covers every bullet Round 1 listed: wrappers present as functions in a fresh shell; no eligible route invoking the real client with an empty AgentDeck trace, then `provider use --via` inside that same live shell flipping to injected values with exactly two resolver calls and no restart; `shell remove`'s printed deactivation command actually evaluated both with the functions present and with them absent; the startup file restored byte-identical to its pre-setup content including the unrelated completion block; a fresh shell with the block installed and `agentdeck` off `PATH` producing an empty normalized transcript while both clients run unchanged; a no-argument `shell setup` in a home holding two shells configuring both and leaving the third with no startup file, repeated for each invoking shell; and the interactive path Round 1 flagged as needing a pty — a first `--via` switch configuring the shell and reporting per file, with a fresh shell then loading the wrappers, versus the same switch under `--quiet` writing no startup file at all and leaving a later shell with no wrappers. Each probe writes a sentinel only after its last assertion and the driver requires that sentinel, so a probe failure cannot be swallowed by `script`'s exit status.
  - [CLOSED] The disclosure conflict is settled in the plan rather than left implicit. `docs/plans/shell-integration.md` keeps the exact table where the host and method are recorded, replaces the marker-absent row's negative delta with the structural fact that the gate starts no AgentDeck process, and records the decision that the four user-facing texts carry the cost shape plus a host-qualified order of magnitude. The Homebrew caveat, `scripts/manage-install.sh`, `README.md`, and `docs/specs/cli-manual.md` now say no AgentDeck process while the marker is absent, one process and one read-only database access while it is present, and "roughly 0.1-0.2 seconds per invocation" on the named host with an explicit "not a performance guarantee"; both assertion sets pin that wording instead of the exact figures.
- Findings: none at P1 or P2 severity.
  - [nit] The acceptance suite's `agentdeck` off `PATH` case cannot exercise the wrapper's own presence guard, only the managed block's. With `agentdeck` absent the block's `command -v agentdeck` test short-circuits, so the wrappers are never defined and the wrapper body is unreachable. Confirmed by mutation: reverting the wrapper guard leaves this suite green. That is not a hole — `scripts/test-completion-install.sh`'s `unavailable` mode sources the generated script directly and does catch it, as does `TestShellInitEmitsDynamicClientWrappersForSupportedShells` — but the case protects the block guard, not the wrapper guard, and a later reader should not assume otherwise.
  - [nit] `scripts/manage-install.sh:512-517` indents the new `printf` continuation arguments with six spaces where the surrounding ones use four. Output is unaffected because the indentation is outside the quoted arguments.
- Test review:
  - The suite was mutation-tested rather than taken at face value. Replacing the guarded zsh deactivation with the unguarded `unfunction codex claude` — the exact hazard the plan singles out — fails it with `(eval):unfunction:1: no such hash table element: codex`, so the deactivation probe genuinely protects that contract. Removing the managed block's presence guard also fails it.
  - `TestShellRemoveSummaryPrintsGuardedDeactivationOncePerShell` pins the three per-shell snippets and the once-per-shell emission at the renderer level, complementing the end-to-end evaluation.
  - `writeShellLifecycleSummary` printing the deactivation line is new product behavior introduced to close Round 1's finding, and it is the behavior the plan's `shell remove` section already specified; it emits for `Removed` and `Absent`, deduplicated per shell, and stays out of JSON output.
- Evidence:
  - `GOCACHE=/private/tmp/agent-deck-go-build go test -count=1 -mod=vendor ./...` passed.
  - `GOCACHE=/private/tmp/agent-deck-go-build go vet -mod=vendor ./...` passed.
  - `GOCACHE=/private/tmp/agent-deck-go-build make check-install` passed and executed `scripts/test-shell-integration-acceptance.sh`.
  - `GOCACHE=/private/tmp/agent-deck-go-build make check-release-distribution` passed.
  - Mutation runs described above, each in an isolated copy of the tree, removed afterwards.
- Verdict: PASS
