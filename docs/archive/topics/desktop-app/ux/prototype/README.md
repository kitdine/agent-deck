# Prototype moved

The interactive prototype that this topic's `ux/` documents cite now lives at
[`/prototype/`](../../../../../../prototype/), at the repository root.

It moved on 2026-08-20 because a prototype is one specimen of the whole product
surface, not an asset of one topic. `work-signals` needs the same specimen, and
a second copy under a second topic would have produced two design truths.

This topic's documents still cite the old path in places. They are rewritten
against the delivered implementation once `desktop-app` finishes; until then,
read the path above.
